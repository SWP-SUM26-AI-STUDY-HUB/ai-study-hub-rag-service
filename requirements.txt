# --- FastAPI & Web Server ---
fastapi>=0.110.0,<0.116
uvicorn>=0.28.0,<0.35
python-multipart>=0.0.9
pydantic>=2.7,<3

# --- LangChain ecosystem (PINNED to 0.3.x — code targets 0.3 API) ---
# Do NOT remove the upper bound: langchain 1.x removed `langchain.storage`,
# `langchain.retrievers` reshuffled, etc. The code must run on 0.3.x.
langchain>=0.3.0,<0.4
langchain-core>=0.3.0,<0.4
langchain-community>=0.3.0,<0.4
langchain-text-splitters>=0.3.0,<0.4
langchain-google-genai>=2.0.0,<3

# --- Vector Database & Storage ---
# Project uses a custom PostgresVectorStore over pgvector via psycopg2.
# chromadb / langchain-chroma are intentionally NOT required.
psycopg2-binary>=2.9.0,<3

# --- Document Loaders & Utilities ---
pypdf>=4.1.0,<6
docx2txt>=0.8

# --- Retrieval Phase Dependencies ---
rank_bm25>=0.2.2
python-dotenv>=1.0.0